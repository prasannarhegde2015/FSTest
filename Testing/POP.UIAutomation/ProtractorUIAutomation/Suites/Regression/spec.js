var PageObject=require('../PageObjects.json');
var env=require('../envconfig.json');
var utility=require('../utility.js');
var utility=require('../Datas.json');

describe('WellConfiguration||Configuration', function() {
	beforeEach(function() {
			originalTimeout = jasmine.DEFAULT_TIMEOUT_INTERVAL;
			jasmine.DEFAULT_TIMEOUT_INTERVAL = 100000;
		});
	afterEach(function() {
			
		
		});
	beforeAll(function() {
			
		});
	afterAll(function() {
		
		});
	
	it('RRL Creation', function() {	
			

	});
	it('ESP Creation', function() {	
			

	});
	
});
describe('Network Configuration||Configuration', function() {
	beforeEach(function() {
			originalTimeout = jasmine.DEFAULT_TIMEOUT_INTERVAL;
			jasmine.DEFAULT_TIMEOUT_INTERVAL = 100000;
		});
	afterEach(function() {
			
		});
	beforeAll(function() {
			
		});
	afterAll(function() {
		
		});
	
	it('Check RRL Network', function() {
		

	});
	
});
describe('WellAnalysis||Optimization', function() {
	beforeEach(function() {
		originalTimeout = jasmine.DEFAULT_TIMEOUT_INTERVAL;
		jasmine.DEFAULT_TIMEOUT_INTERVAL = 100000;
	});
	afterEach(function() {
		
	});
	beforeAll(function() {
		
	});
	afterAll(function() {
		
	});

	it('ESP Analysis', function() {


	});

});